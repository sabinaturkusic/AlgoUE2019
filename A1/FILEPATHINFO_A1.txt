Wurde unter Windows in Visual Studio implementiert.

Executables sind in dem jeweiligen übergeordneten Ordner Debug zu finden.

Die .cpp Datei ist in dem Unterordner "efficient" bzw. "inefficient" zu finden.

Runtime wurde über die Powershell mit dem Tool ptime gemessen.

Es gibt zu time keine in-built Funktion unter Windows.

Runtime Daten wurden in Excel ausgewertet und als PDF gespeichert.